
- [基本概念](#基本概念)
- [礼让 🆚 插队](#礼让-插队)
- [守护线程](#守护线程)
- [线程终止](#线程终止)
- [互斥锁](#互斥锁)
- [死锁](#死锁)
- [释放锁](#释放锁)

## 基本概念

## 礼让 🆚 插队

`yield`：线程的礼让。让出cpu，让其他线程执行，但礼让的时间不确定，所以也不一定礼让成功

`join`：线程的插队。插队的线程一旦插队成功，则肯定先执行完插入的线程的所有任务。

```java
public class Thread01 {
    public static void main(String[] args) {
        T t = new T();
        t.start();

        for (int i=1; i<=20; i++) {
            Thread.sleep(10 * 1000);
            System.out.println("主线程" + Thread.currentThread().getName());

            if(i == 5) {
                System.out.println("主线程让子线程先");
                t.join();  // 插队
                System.out.println("子线程完成主线程继续");
            }
        }
    }
}
```

## 守护线程

用户线程：也叫工作线程，当线程的任务执行完或通知方式结束；

守护线程：一般是为工作线程服务的，当所有的用户线程结束，守护线程**自动结束**。常见的守护线程如：垃圾回收机制

```java
public class Thread04 {
    public static void main(String[] args) {
        MyDaemon daemon = new MyDaemon();
        // 如果希望主线程结束子线程自动结束，只需将子线程设置为守护线程
    	daemon.setDaemon(true);
        daemon.start();

        for (int i=1; i<=20; i++) {
            System.out.println("主线程");
            Thread.sleep(10 * 1000);
        }
    }
}
```

## 线程终止

当线程完成任务后，会自动退出。还可以通过使用变量来控制run方法退出的方式停止线程，即通知方式

```java
class Cat extends Thread {
    private boolean loop = true;

    @Override
    public void run() {
        while (loop) {
            System.out.println("miao~" + Thread.currentThread().getName());  // Thread-0
            Thread.sleep(1000);
        }
    }

    public void setLoop(boolean loop) {
        this.loop = loop;
    }
}

public class Thread01 {
    public static void main(String[] args) {
        Cat cat = new Cat();
        cat.start();
        Thread.sleep(10 * 1000);
        cat.setLoop(false);
    }
}
```

# 生命周期

JDK中用Thread.State枚举类表示了线程的几种状态，用`getState`方法查看当前状态

`NEW`：尚未启动

`RUNNABLE`：在Java虚拟机中执行

- 又有Ready和Running两个子状态

`BLOCKED`：被阻塞等待监视器锁定

`WAITING`：正在等待另一个线程执行特定动作

`TIMED_WAITING`：正在等待另一个线程执行动作达到指定等待时间

`TERMINATED`：已退出

![](https://cdn.nlark.com/yuque/0/2023/png/2341594/1679992840096-20098c2e-a90d-460e-809a-db000d49d0f5.png)

# 同步机制

在多线程编程中，一些敏感数据不允许被多个线程同时访问，此时就使用同步访问技术，保证数据在任何同一时刻，最多有一个线程访问，以保证数据的完整性。

也可以这么理解：线程同步，即当有一个线程对内存进行操作时，其他线程都不可以对这个内存地址进行操作，直到该线程完成操作，其他线程才能对该内存地址进行操作。

实现方法：synchronized关键字

```java
// 同步代码块，得到对象的锁才能操作同步代码
public void sell() {
    synchronized (this) {
        if (ticketNum <= 0) {
            System.out.println("停止售票...");
            loop = false;
            return;
        }
    }
}

// 同步方法，同时只能有一个线程执行同步方法
public synchronized void sell() {
    if (ticketNum <= 0) {
        System.out.println("停止售票...");
        loop = false;
        return;
}
```

## 互斥锁

Java语言中，引入对象互斥锁的概念，来保证共享数据操作的完整性。每个对象都对应于一个可称为“互斥锁”的标记，这个标记用来保证在任一时刻，只能有一个线程访问该对象。

关键字synchronized来与对象的互斥锁联系，当某个对象用synchronized修饰时，表明该对象在任一时刻只能有一个线程访问。

同步的局限性：导致程序的执行效率降低。

- 非静态同步方法的锁可以是`this`，也可以是其他对象（要求是**同一对象**）；
- 静态同步方法的锁为当前类本身（`类.class`）

实现步骤：

- 先分析上锁的代码；
- 选择同步代码块（推荐）或同步方法；
- 要求多个线程的锁对象为同一个即可

```java
public synchronized static void sell() { ... }

public static void sell() {
    synchronized (SellTicket.class) { ... } // 当前类本身
}
```

## 死锁

多个线程都占用了对方的锁资源，但不肯相让，导致了死锁。要注意避免死锁发生。

```java
if (flag) {
    synchronized (o1) {
        System.println("get o1");
        synchronized (o2) {
            System.println("get o2");
        }
    }
} else {
    synchronized (o2) {
        System.println("get o2");
        synchronized (o1) {
            System.println("get o1");
        }
    }
}
```

## 释放锁

下面的操作会释放锁：

- 当前线程的同步方法/同步代码块执行结束（_上厕所，完事出来_）；
- 当前线程在同步方法/同步代码块中遇到`break`、`return`（_没有正常完事，经理叫他改bug，不得已出来_）；
- 当前线程在同步方法/同步代码块中出现了未处理的异常，导致一场结束（_没有正常完事，发现没带纸，不得已出来_）；
- 当前线程在同步方法/同步代码块中执行了线程对象的`wait`方法，当前线程暂停，并释放锁（_没有正常完事，觉得还要酝酿下，出来等会进去_）

下面的操作不会释放锁：

- 线程执行同步代码块/同步方法时，程序调用`sleep`、`yield`方法，暂停当前执行（_上厕所太困了，在坑位眯了一会儿，没出来_）；
- 线程执行同步代码块时，其他线程调用了该线程的`suspend`方法将该线程挂起（_尽量避免使用`suspend`、`resume`来控制线程_）

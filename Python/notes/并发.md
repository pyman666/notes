
- [概念](#概念)
- [比较](#比较)
- [协程](#协程)
- [GIL](#gil)
- [生命周期](#生命周期)
- [threading](#threading)
- [queue](#queue)
- [lock](#lock)
- [pool](#pool)
- [multiprocessing](#multiprocessing)
- [asyncio](#asyncio)

## 概念

## 比较

|  | **进程（multiprocessing）** | **线程（threading）** | **协程（asyncio）** |
| --- | --- | --- | --- |
| **优点** | 可利用多核CPU | 相较于进程更轻，占用资源少 | 内存开销最少，启动数量最多 |
| **缺点** | 占用资源多，数量少 | 1）多线程只能并发执行，不能利用多核CPU（GIL） 2）线程切换开销 | 支持多库有限（aiohttp），代码复杂 |
| **适用** | CPU密集型 | IO密集型，同时运行多任务数量要求不多 | IO密集型，有超多任务运行，有库支持 |

## 协程

异步IO，在单线程内实现并发，核心是：**超级循环**

## GIL

GIL，全局解释器锁，（global interpreter lock），其使得任何时刻只有一个线程在运行。

## 生命周期

---

# 代码

## threading
```python
import threading

def foo(url): pass

threads = []
for url in urls:
	t = threading.Thread(target=foo, args=(url,))
    threads.append(t)
for t in threads:
	t.start()  # 启动线程
for t in threads:
	t.join()  # 等待结束
```
## queue

可用于多线程间线程安全的数据通信
```python
import queue

q = queeu.Queue()
q.put(i)  # add item
i = q.get()  # get item
q.qsize()  # num of item
q.empty()  # wheather empty
q.full()  # wheather full

# 生产者消费者模式，有数据丢失？？？？

def producer(url_queue, html_queue):
    while True:
        url = url_queue.get()
        html = do(url)
        html_queue.put(html)
        print(threading.current_thread().name, url_queue.qsize())

def consumer(html_queue):
    while True:
        html = html_queue.get()
        results = do(html)
        for i i results:
            file.write(i)
        print(threading.current_thread().name, html_queue.qsize())

for i in range(3):
    t = threading.Thread(target=producer, args=(url_queue, html_queue), name=f"p{i}")
    t.start()

for i in range(3):
    t = threading.Thread(target=consumer, args=(html_queue), name=f"c{i}")
    t.start()
```
## lock
```python
lock = threading.Lock()

def foo():
    with lock:
        # some dangerous codes
```
## pool

线程池：新建线程需要分配资源，结束线程需要回收资源，利用线程池可以减少新建和回收的开销。适用于处理突发性大量请求/需要大量线程且单个任务时间短。具有**防御功能**：能够避免系统因创建线程过多导致负荷过大。
```python
from concurrent.futures import ThreadPoolExecutor, as_completed

# map，简洁
with ThreadPoolExecutor() as pool:
    results = pool.map(foo, urls)

# future模式，更强大
with ThreadPoolExecutor() as pool:
    futures = [pool.submit(foo, url) for url in urls]
for future in futures:  # 都完了按顺序返回
    print(future.result())
for future in as_completed(futures):  # 哪个先结束就返回，顺序不定
    print(future.result())
```
## multiprocessing

注意点：进程池的pool因为相互隔离，所以必须要求所有的函数都定义完了，所以必须放在**\_\_main\_\_的最后面**。

## asyncio
```python
import asyncio

loop = asyncio.get_event_loop()  # 获取事件循环

# 定义协程
asyncio def foo(url):
    result = await do(url)  # IO，必须支持异步IO特性

tasks = [loop.create_task(foo(url)) for url in urls]  # 创建task列表
loop.run_until_complete(asyncio.wait(tasks))  # 执行
```

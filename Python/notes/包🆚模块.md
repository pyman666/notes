
- [模块](#模块)
- [查找顺序](#查找顺序)
  - [builtin](#builtin)
  - [sys.path](#syspath)
- [包内导入 ★](#包内导入-)
- [局部导入](#局部导入)
- [覆盖导入](#覆盖导入)
- [循环导入](#循环导入)
- [可选导入](#可选导入)

## 模块

## 查找顺序

第一次导入时：`builtin` → `sys.path`（按顺序，找到就不往下了）；

第二次导入时：从已加载的模块中找`sys.modules`；

### builtin
```python
import builtins

builtins.print("hello world")

dir(builtins)  # py3，查看内置模块
# dir(__builtin__)  # py2
```
### sys.path

`sys.path`构成：

- 当前目录；

- 环境变量`PYTHONPATH`中指定的路径列表；

- 特定路径下的`.pth`文件中的文件路径列表；

- python安装路径的`lib`库；

追加路径的方式：

- 直接修改`sys.path`值（只作用于当前文件，注意优先级）；

- 修改环境变量`PYTHONPATH`（只在shell中有效）；

- 添加`.pth`文件；
```python
import sys

sys.path.append("C:/test.py")

import test
```
# 导入场景

## 包内导入 ★

包内的模块导入包内的模块称为包内导入，可以分为：

- 绝对导入：参照`sys.path`进行路径检索；

- 相对导入：使用`.`来指代相对路径；

使用建议：

- 包内导入：相对导入；

- 包外导入：绝对导入；
```python
'''
pkg/
    __init__.py
    sub_pkg1/
        __init__.py
        moduleX.py
        moduleY.py
    sub_pkg2/
        __init__.py
        moduleZ.py
    moduleA.py
'''

from . import moduleY

from .moduleY import spam as ham

from ..sub_pkg1 import moduleY
from ..sub_pkg2.moduleZ import eggs

from ..moduleA import foo
```
> 每一个relative import，都是先找到它的absolute import，然后再import。

简单来说，就是在相对导入里面，一个模块的位置是由该模块的 `__name__` 属性来确定，如果该属性值不包含任何包的信息，则把当前模块视为顶层模块。在模块里可通过属性 `__package__` 获取自身的包信息，即该模块所在包的结构，诸如 `xxx`、`xxx.yyy.zzz`等形式，第一个节点就是顶层包，如果 `__package__` 为 `None` 表示该模块是顶层模块。

单独运行一个py文件时：

- 会将当前文件所在的目录添加到`sys.path`，之后`sys.path`的内容已经确定；

- 文件`__name__`为`__main__`，`__package__`为`None`，为顶层执行文件，不能使用相对导入；

## 局部导入

有时候在局部import可以节约资源、提高效率：
```python
def foo():
    import m  # 若foo没有使用，就不会import
    print(m.name)
```
## 覆盖导入

应避免使用覆盖导入。

当自己的模块名与内置模块重名时，可以使用`from x import xx` 来强制指定使用自己的模块，否则会被内置模块覆盖：
```python
import sys  # 内置

from my_pkg import sys  # 个人
```
## 循环导入

应避免使用循环导入。

a中导入b，b中导入a，可能会引起代码重复执行、或者变量未定义等问题：
```python
# name = 'a'  # 重复打印

import b
print(b.name)

name = 'a'  # 报错
```
```python
# name = 'b'

import a
print(a.name)

name = 'b'
```
## 可选导入

a、b模块功能相似，优先导入a，不存在则导入b：
```python
try:
    import a as m
catch ModuleNotFoundError:
    import b as m

print(m.name)
```
